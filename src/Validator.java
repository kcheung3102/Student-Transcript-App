public class Validator {

    private static boolean isLettervalid(String userInput){
        return userInput.matches("[a-zA-F]+");
    }

    private static boolean isInteger(int credit ) {
        return
    }
}
